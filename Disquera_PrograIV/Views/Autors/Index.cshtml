@model IEnumerable<Disquera_PrograIV.Models.Autor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@if (Request.IsAuthenticated && User.IsInRole("Ejecutivo"))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.aut_nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aut_ape)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nacionalidad.nac_nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.aut_nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aut_ape)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nacionalidad.nac_nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.aut_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.aut_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.aut_id }) |
            @Html.ActionLink("Ver Discos ", "dis_autor", new { id = item.aut_id })
        </td>
    </tr>
}

</table>
