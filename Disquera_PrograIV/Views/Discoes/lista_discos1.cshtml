@model IEnumerable<Disquera_PrograIV.Models.Disco>

@{
    ViewBag.Title = "lista_discos1";
}
<br/>
<h2>Disco del Autor : @ViewBag.autor @ViewBag.autor_Ape</h2>

@if (Request.IsAuthenticated && User.IsInRole("Ejecutivo"))
{
<p>
    @Html.ActionLink("Create New", "Create")
</p>}
else if (Request.IsAuthenticated && User.IsInRole("Cliente"))
{

}
<table class="table">
    <tr>
        <th>
         Album
        </th>
        <th>
            Valor
        </th>
   
        <th>
           Genero
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dis_alb)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dis_val)
        </td>
    
        <td>
            @Html.DisplayFor(modelItem => item.Genero.gen_nom)
        </td>
        <td>
            @if (Request.IsAuthenticated && User.IsInRole("Ejecutivo"))
            {
                @Html.ActionLink("Edit", "Edit", new { id = item.dis_id })
                @Html.ActionLink("Details", "Details", new { id = item.dis_id })
                @Html.ActionLink("Delete", "Delete", new { id = item.dis_id })
            }
            else if (Request.IsAuthenticated && User.IsInRole("Cliente"))
            {

            }
        </td>
    </tr>
}

</table>
